{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Innie Scheduler (GCC)",
            "command": "gcc",
            "args": [
                "-Wall",
                "-Wextra",
                "-g",
                "-I${workspaceFolder}/include",
                "${workspaceFolder}/src/*.c",
                "-o",
                "${workspaceFolder}/build/arena-allocator.exe",
                "-pthread"
            ],
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "problemMatcher": [
                "$gcc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
    ]
}